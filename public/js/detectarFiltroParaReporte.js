(function () {
    const d = document,
        $botonGenReporte = d.querySelector(".filtro-subtitulo-reporte"),
        $formulario = d.querySelector(".container-filter form"),
        $inputs = d.querySelectorAll(".container-filter form [data-input]");

        console.log($formulario);

        
})();